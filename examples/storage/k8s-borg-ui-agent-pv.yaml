apiVersion: v1
kind: PersistentVolume
metadata:
  name: k8s-borg-ui-agent-pv
spec:
  capacity:
    storage: 10Mi
  accessModes:
  - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: nfs.example.com
    path: /storage/shared/cluster/static/k8s-borg-ui-agent
