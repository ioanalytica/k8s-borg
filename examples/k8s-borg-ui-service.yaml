---
# borg-ui Service — ClusterIP fronting the web UI (container port 8081).
apiVersion: v1
kind: Service
metadata:
  name: k8s-borg-ui
  labels:
    app: k8s-borg-ui
    release: k8s-borg
spec:
  selector:
    app: k8s-borg-ui
  ports:
    - name: http
      port: 8081
      targetPort: http
