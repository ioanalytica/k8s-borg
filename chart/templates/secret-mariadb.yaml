{{- /*
Copyright IO ANALYTICA. All Rights Reserved.
SPDX-License-Identifier: Apache-2.0
*/}}
{{- if and .Values.databases.mariadb.enabled (not .Values.databases.mariadb.existingSecret) }}
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ printf "%s-mariadb" (include "common.names.fullname" .) }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "common.labels.standard" (dict "customLabels" .Values.commonLabels "context" $) | nindent 4 }}
stringData:
  {{- range .Values.databases.mariadb.instances }}
  {{ .name }}.conf: |
    {{- .config | nindent 4 }}
  {{- end }}
{{- end }}
